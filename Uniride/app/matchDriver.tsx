import { ScrollView, StyleSheet, Text } from "react-native";

export default function MatchDriver() {
  return (
    <ScrollView>
      <Text>MatchDriver</Text>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {},
});
